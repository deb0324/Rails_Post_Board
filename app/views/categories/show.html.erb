<%= render 'shared/title', title: @category.title %>

<div>
  <% @category.posts.each do |post| %>
    <div>
      <h3><%= post.title %></h3>
      <p><%= post.content %></p>

      <%= link_to "show post", post_path(post) %>
      
    </div>
  <% end %>
</div>